import { RecipeSupplyAssembler } from './recipe-supply.assembler';

describe('RecipeSupplyAssembler', () => {
  it('should create an instance', () => {
    expect(new RecipeSupplyAssembler()).toBeTruthy();
  });
});
