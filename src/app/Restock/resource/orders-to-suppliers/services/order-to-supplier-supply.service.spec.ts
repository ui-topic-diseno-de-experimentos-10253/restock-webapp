import { OrderToSupplierSupplyService } from './order-to-supplier-supply.service';

describe('OrderToSupplierSupplyService', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierSupplyService()).toBeTruthy();
  });
});
