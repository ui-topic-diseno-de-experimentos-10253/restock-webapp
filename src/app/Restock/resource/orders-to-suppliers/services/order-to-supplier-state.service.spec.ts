import { OrderToSupplierStateService } from './order-to-supplier-state.service';

describe('OrderToSupplierStateService', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierStateService()).toBeTruthy();
  });
});
