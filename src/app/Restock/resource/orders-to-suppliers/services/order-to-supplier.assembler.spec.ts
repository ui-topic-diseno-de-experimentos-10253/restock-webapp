import { OrderToSupplierAssembler } from './order-to-supplier.assembler';

describe('OrderToSupplierAssembler', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierAssembler()).toBeTruthy();
  });
});
