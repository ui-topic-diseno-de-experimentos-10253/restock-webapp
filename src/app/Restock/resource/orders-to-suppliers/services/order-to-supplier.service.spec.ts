import { OrderToSupplierService } from './order-to-supplier.service';

describe('OrderToSupplierService', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierService()).toBeTruthy();
  });
});
