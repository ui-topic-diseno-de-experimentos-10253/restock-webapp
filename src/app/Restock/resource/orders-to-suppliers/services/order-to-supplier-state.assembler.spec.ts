import { OrderToSupplierStateAssembler } from './order-to-supplier-state.assembler';

describe('OrderToSupplierStateAssembler', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierStateAssembler()).toBeTruthy();
  });
});
