import { OrderToSupplierSupplyAssembler } from './order-to-supplier-supply.assembler';

describe('OrderToSupplierSupplyAssembler', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierSupplyAssembler()).toBeTruthy();
  });
});
