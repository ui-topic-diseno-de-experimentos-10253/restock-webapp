import { OrderToSupplierSituationService } from './order-to-supplier-situation.service';

describe('OrderToSupplierSituationService', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierSituationService()).toBeTruthy();
  });
});
