import { OrderToSupplierSituationEntity } from './order-to-supplier-situation.entity';

describe('OrderToSupplierSituationEntity', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierSituationEntity()).toBeTruthy();
  });
});
