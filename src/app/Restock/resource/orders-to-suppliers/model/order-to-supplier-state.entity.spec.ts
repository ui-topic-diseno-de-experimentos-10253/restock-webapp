import { OrderToSupplierStateEntity } from './order-to-supplier-state.entity';

describe('OrderToSupplierStateEntity', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierStateEntity()).toBeTruthy();
  });
});
