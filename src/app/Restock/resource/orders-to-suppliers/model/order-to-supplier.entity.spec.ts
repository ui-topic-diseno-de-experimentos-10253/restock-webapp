import { OrderToSupplierEntity } from './order-to-supplier.entity';

describe('OrderToSupplierEntity', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierEntity()).toBeTruthy();
  });
});
