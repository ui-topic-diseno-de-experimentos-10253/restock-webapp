import { OrderToSupplierSupplyEntity } from './order-to-supplier-supply.entity';

describe('OrderToSupplierSupplyEntity', () => {
  it('should create an instance', () => {
    expect(new OrderToSupplierSupplyEntity()).toBeTruthy();
  });
});
